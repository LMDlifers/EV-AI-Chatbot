import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "ZapVite",
  initialMessages: [createChatBotMessage("Hello! Ask me anything EV-charging location related and I will help you!")],
};

export default config;
